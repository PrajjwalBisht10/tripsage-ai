# Hoist specific packages to root node_modules for compatibility
# sharp: Required by Next.js for image optimization
public-hoist-pattern[]=*sharp*
# @tailwindcss/oxide: Required by @tailwindcss/postcss (Tailwind CSS v4)
public-hoist-pattern[]=*@tailwindcss/oxide*

# Do not auto-install peers (avoids pulling unused native/tooling chains).
auto-install-peers=false
