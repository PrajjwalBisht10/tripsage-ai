/**
 * @fileoverview Test constants for Upstash mocks.
 *
 * Use these instead of hardcoded strings for clarity and maintainability.
 * These tokens are only used in test environments and have no production value.
 */

export const TEST_QSTASH_TOKEN = "test-qstash-token";

export const TEST_QSTASH_SIGNING_KEY = "test-signing-key";

export const TEST_QSTASH_NEXT_SIGNING_KEY = "test-next-signing-key";

export const TEST_REDIS_URL = "https://test-redis.upstash.io";

export const TEST_REDIS_TOKEN = "test-redis-token";
